# rag

> RAG 原子能力边界。

## 定位

`rag/` 只放检索增强生成的基础元件。

## 允许内容

- chunking
- embedding
- vector access
- retrieval pipeline primitives

## 禁止内容

- mission/session recall policy
- app 领域知识编排

召回策略和记忆语义属于 `ai-harness`。

/**
 * Analyst stage agent — barrel export
 *
 * 跨维度综合：从 reconciler 的 factTable + researcher findings 生成 insights /
 * contradictions / gaps，喂给 Writer。
 */

export { AnalystAgent } from "./analyst.agent";
export { QuickViewSynthesizerAgent } from "./quick-view-synthesizer.agent";

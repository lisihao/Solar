# 微信公众号正文格式规范（content-transformer 与 polish-reviewer 共用的单一真源）

正文必须改写成**结构完整、质量过硬的微信公众号深度长文**，按以下骨架组织。

## 质量第一（比字数更重要——字数靠实质达到，绝不灌水）

字数是底线，**质量才是关键**。达标字数必须靠**实质内容**自然写到，不是同义反复、空话套话凑数：

- **信息增量**：每一节都给读者新东西（新角度 / 新解释 / 新例子），**不重复、不绕圈、不灌水**。
- **准确**：完全忠于原文事实 / 数据 / 结论，**不编造、不夸大**；拿不准的不写。
- **可读**：逻辑连贯、过渡自然、有节奏，像**人写的科普**，不是 AI 八股（别堆"综上所述""值得注意的是"等套话）。
- **有洞察**：把"是什么"讲透，并答"所以呢 / 为什么重要 / 对谁有用"，外行看懂、内行不觉浅。
- **反灌水红线**：某点确实没料，就把别的点写深，**绝不靠重复 / 废话 / 拆长句凑字数**。宁可一节扎实 400 字，不要两节各 150 字的水文。

## 结构骨架

1. **金句开篇（若原文有有力观点 / 引言）**：开头放 2–4 句最有冲击力的核心金句，每句后用 `—— 出处 / 人名` 标注（无明确出处则省略本段）。
2. **导语 / 背景介绍**：一段【导读】或「背景介绍」，交代主题、背景、为什么值得读（钩子切入，≤ 200 字，可同时作 digest）。
3. **角色 / 多方观点介绍（若涉及人物 / 多方立场）**：简介关键人物或各方立场，各一两句。
4. **目录（正文 ≥ 4 节时必给）**：编号列出各节小标题，便于长文导航。
5. **正文分节**：**编号 `## 小标题`（如 `## 1. xxx`、`## 2. xxx`）**，每节聚焦一个论点并充分展开。
6. **结尾**：小结 / 启示 + 一句引导互动（点赞 / 在看 / 转发 或关注提示）。**不要自行编造「参考资料」或外链**——真实来源会由系统自动附加在文末。

## 排版要求

- **段落短（每段 1–3 句、多留白）**，但**每节要由多个短段组成、凑足篇幅**——短段 ≠ 少内容；**严禁一节只写一两句就跳下一节**。
- 关键术语 / 数字用「」或 **加粗** 强调；适合枚举处用 `-` 列表分点。
- 口语化但专业、逻辑清晰。

## 深度展开（决定成败——素材越短越要展开，绝不能几句带过）

素材常只有摘要级长度，但你必须产出**有信息密度的长文**：

- 把素材的**每个要点单独成节、逐点深入展开**，不要一句带过；
- 对每个专业术语 / 方法 / 概念，用**通俗类比 + 例子**讲清楚，让外行看懂；
- 逐节讨论：**这问题为什么重要、难在哪、做法如何运作、结果说明了什么、对谁有用、有什么局限与未来方向**；
- 以上都是对素材的**合理科普与阐释**，不是编造——只要不杜撰新的事实 / 数据 / 结论即可。

## 字数与自查（硬性要求）

- **每个 `##` 小标题下正文至少 300 字**（拆成 3–5 个短段：论点 + 通俗解释 + 例子/类比 + 为什么重要）。**只写一两句的节不合格。**
- **全文起码 2000 字以上**（≈ 6–8 节 × 每节 ≥ 300 字；目标 2000–3500；硬性下限 1500）。**宁可详尽，不可简陋——但详尽必须靠实质内容，不靠灌水（见上「质量第一」）。**
- **常见错误（必须避免）**：只搭了一堆小标题骨架、每节却只有一两句——这是"简陋"，不是"长文"。骨架之外**必须把每节填满**。
- **自查**：任何一节 < 300 字 / 全文 < 2000 字 / 分节 `##` < 3，**必须继续逐节扩写后再输出**。
- **若 input 含 `expandDirective`（非空）**：说明上一稿过短，必须严格遵照它大幅扩写后再输出。
- **所有事实 / 数据 / 结论 / 引用必须源自原文，不得编造。**

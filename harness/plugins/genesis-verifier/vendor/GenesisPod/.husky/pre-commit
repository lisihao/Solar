#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 加大 Node 堆：type-aware eslint（lint-staged）多文件提交易 OOM（默认 ~2GB）。
# 8GB 实测可覆盖全仓最大单次 staged 提交。允许外部覆盖（已设更大值则保留）。
export NODE_OPTIONS="${NODE_OPTIONS:---max-old-space-size=8192}"

npx lint-staged --concurrent false --max-arg-length 30000

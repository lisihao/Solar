# 依赖
node_modules/

# Generated SDK output
sdk/
.pnp
.pnp.js
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/
!backend/src/modules/ai-harness/runner/env/
!backend/src/modules/ai-harness/runner/env/**

# ESLint cache
.eslintcache
backend/.eslintcache
frontend/.eslintcache

# 测试
coverage/
coverage-*/
*.lcov
.nyc_output
.pytest_cache/
htmlcov/
.playwright-mcp/

# 生产构建
.next/
out/
dist/
build/

# 环境变量
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local

# 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# 编辑器
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json.example
.idea/
*.swp
*.swo
*~
.DS_Store

# Prisma
prisma/migrations/**/migration.sql

# 数据库数据（Docker volumes）
data/
!.claude/skills/data/
!**/src/**/data/
!frontend/app/**/data/
*.db
*.sqlite

# 临时文件
tmp/
temp/
*.tmp

# Claude Code logs, orchestrator state, and sub-agent worktrees
.claude/logs/
.claude/orchestrator/*.json
.claude/worktrees/

# 操作系统
.DS_Store
Thumbs.db

# 安全敏感文件
*.pem
*.key
*.crt
*.p12
*.pfx
*credentials*.json
*secret*.json
service-account*.json

# 调试文件
debug/

# One-off data cleanup backups (keep local, never commit — may contain PII)
**/.cleanup-backup/

# TypeScript 构建信息
*.tsbuildinfo

# 其他
*.seed
.cache

# OCI sensitive files
.oci/
infra/oci/terraform/terraform.tfvars
infra/oci/terraform/terraform.tfstate
infra/oci/terraform/terraform.tfstate.backup
infra/oci/terraform/.terraform/
infra/oci/terraform/tfplan
infra/oci/terraform/*.tfplan

# Tencent Cloud EdgeOne
.env
.edgeone/*
!.edgeone/project.json
.tef_dist/*
  
# Generated files (changelog.json is tracked for deployment)
frontend/lib/generated/*
!frontend/lib/generated/changelog.json
# 4.5MB CHANGELOG.md was previously written to frontend root and tracked in
# git, bloating diffs. Moved to lib/generated/ (PR-X35) and untracked.
/CHANGELOG.md
frontend/lib/generated/CHANGELOG.md

# UI Patrol outputs (generated, not checked in)
.ui-patrol/screenshots/
.ui-patrol/reports/
.ui-patrol/evaluations/
.ui-patrol/baselines/
.ui-patrol/diffs/

# Topic Insights golden runner transient outputs（每次 run 都会生成新文件，不入库）
backend/fixtures/golden-reports/

# Backend runtime output dirs (created by Dockerfile mkdir -p, hold transient
# data: PPT/Word exports + image/PDF thumbnails)
backend/exports/
backend/public/

# Jest coverage products that have leaked into src/ in the past (PR-X29)
backend/src/coverage-supplemental/

.aider*
.claude/settings.local.json
.claude/CLAUDE.local.md
tmpclaude-*

# Secrets / captured auth state (never commit — see SECURITY.md)
e2e/.auth/
backend/.env.railway
*.gitleaks-report.json
.gitleaks-report.json
.gitleaks-tree.json
.tmp-gitleaks/

# Internal AI-collaboration memory (incident postmortems, personal prefs) — not published
.claude/memory/

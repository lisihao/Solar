route: /rag
tier: important
auth: demo

expected_structure:
  main:
    - knowledge_base_list: true

data_states:
  with_data:
    assertions:
      - "Page contains at least 1 knowledge base"
      - "Each KB has: name, status, document count"
  empty:
    assertions:
      - "Shows empty state or create prompt"

forbidden:
  - "[object Object]"
  - "undefined"
  - "NaN"

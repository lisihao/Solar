# 根目录 .dockerignore —— 全局兜底，防止源码 / 历史 / 内部文档进任何 build context
#
# 各服务的 Dockerfile build context 通常是 backend/ frontend/ ai-service/，
# 那时优先用各自目录的 .dockerignore。此文件仅在 context 设到根目录时生效。

# Git / IDE / OS
.git
.gitignore
.gitattributes
.github
.vscode
.idea
.DS_Store
Thumbs.db

# Claude / AI 助手内部上下文（敏感，禁止进镜像）
.claude
CLAUDE.md
**/CLAUDE.md
memory
**/memory

# 文档 / 标准 / 设计稿（业务源码，不该交付给客户）
docs
standards
plans
designs
notes
*.md
!README.md

# 临时 / 构建产物 / 依赖
node_modules
**/node_modules
dist
**/dist
build
**/build
.next
**/.next
coverage
**/coverage
*.tsbuildinfo

# 环境 / 密钥（绝对不能进镜像）
.env
.env.*
!.env.example
!.env.production.example

# 日志 / 缓存
*.log
**/*.log
.cache
**/.cache

# 测试 / 脚本工具
**/tests
**/__tests__
**/__mocks__
**/*.spec.ts
**/*.test.ts

# 子项目
ai-service
infra

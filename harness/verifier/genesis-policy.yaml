schema_version: solar.verifier.genesis_policy.v1
gate_mode: warn

# Phase 2-ready policy surface. Default stays warn so historical Solar-Harness
# work does not suddenly block. Flip individual modes to `blocker` after a
# reviewed baseline ratchet.
blocker_rules:
  - rule_id: genesis.arch.layer_boundaries
    mode: warn
  - rule_id: genesis.vocab.purity
    mode: warn
  - rule_id: genesis.session.projector_purity
    mode: warn
  - rule_id: genesis.arch.harness_uplift_gate
    mode: warn
  - rule_id: genesis.waiver.exception_registry
    mode: warn
  - rule_id: genesis.contract.baseline_ratchet
    mode: warn

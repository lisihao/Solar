# Solar-Port Notes

This first Solar port keeps the same file set as GenesisPod raw standards.
Path and domain adaptation is handled by
`harness/verifier/standards/rule_packs/genesispod_standards/path_adapter.yaml`.

Rules that do not apply to Solar-Harness first-phase scope must be emitted as
`not_applicable`, never silently skipped.
